var actMoveDo = actMoveDo || (function($) {
    "use strict";

        function ActMoveDo(settings) {

            var defaults = {

            };

            $.extend(true, defaults, settings || {});

        }

    return ActMoveDo;

}(jQuery));